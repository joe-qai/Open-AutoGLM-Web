1、设备管理，adb devices可以获取所有连接的设备ID
adb -s {设备ID} shell getprop ro.product.model   # 获取设备型号
adb -s {设备ID} shell getprop ro.product.manufacturer  # 获取设备制造商
adb -s {设备ID} shell getprop ro.build.version.release  # 获取设备系统版本号